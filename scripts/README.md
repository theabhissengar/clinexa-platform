# scripts

Operator and developer helper scripts (bootstrap, migrate, seed, env checks).

Populate as database migrations and CI workflows mature. Prefer idempotent scripts documented in the root README.
