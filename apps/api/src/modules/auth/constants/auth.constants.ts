export const AUTH_REFRESH_COOKIE_PATH = '/v1/auth';

/** Nest metadata key for public (unauthenticated) routes. */
export const IS_PUBLIC_KEY = 'isPublic';

/** Passport JWT strategy name. */
export const JWT_STRATEGY = 'jwt';
