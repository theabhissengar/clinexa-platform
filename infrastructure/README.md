# Infrastructure

Placeholder for environment topology and future IaC (Terraform, Pulumi, or Kubernetes manifests).

No cloud resources are provisioned from this directory in the V1 foundation.

| Folder | Intent |
| --- | --- |
| `environments/local` | Local development notes |
| `environments/staging` | Staging topology (future) |
| `environments/production` | Production topology (future) |
